# PAW MAP v0.10.2 Beta — 인수 메모

## v0.10.2 양피지 지도·롱프레스 장소 생성

- 랜덤 지형과 장소별 이모지 장식을 제거하고 양피지 배경과 단순 핀·이름으로 정리했습니다. 저장된 장소나 기록을 삭제하지 않습니다.
- 빈 지도 550ms 누르기 → 손 떼기 → 이름 입력으로 누른 좌표에 장소를 저장합니다. 드래그·두 손가락 조작·터치 취소·채팅 전환은 생성에서 제외합니다.
- `＋ 장소 추가` 버튼도 제공합니다. 판타지 장소는 현실 좌표 추정·주소 조회 없이 생성됩니다.
- 지도 드래그와 두 손가락 확대/축소를 지원하며 새로고침 버튼은 저장된 핀을 초기화하지 않고 화면 중심만 맞춥니다.
- 판타지 바텀시트의 헤더·탭·버튼·리뷰·커뮤니티를 양피지 색상으로 통일했습니다. 일반 모드의 색상은 유지합니다.
- 검증: JavaScript 구문 검사와 `node tests/fantasy-parchment.test.mjs`의 제스처·좌표 저장 모의 테스트 12개. 지도 SVG를 이미지로 렌더링해 배경·핀을 확인했습니다. 실제 SillyTavern/모바일 브라우저 통합 동작과 AI 생성은 이번 환경에서 실행 검증하지 못했습니다.

## v0.10.1 판타지 공용 바텀시트·절차 생성 지도

- 판타지 모드의 패널 색상과 장소 상세 구조를 일반 PAW MAP과 동일하게 통합했습니다.
- 판타지 지도 핀과 저장 장소를 누르면 일반 모드와 같은 바텀시트가 열리며 개요·이벤트·리뷰·내부·약도·커뮤니티를 사용할 수 있습니다.
- 판타지 커뮤니티는 별도의 노란 전체화면을 거치지 않고 장소 바텀시트 안에서 확인·생성합니다.
- 모바일에서 ✨ 버튼의 `touchend`와 `click`이 중복 발화하던 경로를 `pointerup/click` 단일 잠금 처리로 교체했습니다.
- 강·호수·흙길·숲·산맥·작은 마을을 외부 지도 API 없이 채팅별로 생성하는 가상 지도 배경을 추가했습니다.
- 판타지 리뷰는 Google Maps나 현대 SNS를 언급하지 않고 입력된 시대·세계관의 방문담으로 생성합니다.

## v0.10.0 선택형 판타지 지도·세계관 소식망

- 비활성화되어 있던 판타지 모드 토글과 양피지 지도를 다시 활성화했습니다.
- AI 지도 생성은 추가하지 않고, 기존의 수동 장소 추가·핀 배치·이동 기록을 판타지 지도에 그대로 사용합니다.
- `세계관 모드` 자유 입력칸을 추가했습니다. 입력값은 채팅/연결 세계관별로 저장되며 판타지 커뮤니티 생성 프롬프트의 최우선 배경 규칙으로 사용됩니다.
- 판타지 장소 상세에 `세계관 소식 보기` 진입점을 추가했습니다.
- 판타지 소식망은 시대·문화·기술 수준에 맞는 정보 전달 수단과 화자를 사용하고, 확인되지 않은 현대 SNS 용어를 금지합니다.
- 판타지 모드에서는 현실 좌표 기반 Google·Overpass 보강을 사용하지 않습니다.

## v0.9.61 사진 저장·인근 실명·커뮤니티 말투 보강

- 커뮤니티 저장 시 누락되던 `source`, `factIds`, 공개 사진 문구와 출처를 보존해 Pollinations 사진이 실제 표시되도록 수정했습니다.
- Google 조사 결과에 주소 주변 가게·카페·식당·상점·명소·공원·역·건물의 확인된 실명을 최대 8개까지 별도 수집합니다.
- 인근 실명이 있으면 피드 2~3개가 정확한 고유명사를 본문과 `factIds`에 함께 사용했는지 검사합니다.
- 피드에 건조형·급발진형·즉시반응형·수다형·감성형·정보형 말투를 섞고 같은 유형과 첫 단어의 반복을 검사합니다.
- 커뮤니티에서만 반말·음슴체·초성·선택적 욕설·가벼운 오타를 허용하며, 혐오·특정인 공격·확인되지 않은 범죄 주장은 계속 금지합니다.
- 인근 실명 근거가 있는 장소 글을 사진 후보에서 우선하고, 사진 서비스가 모두 실패하면 한 번만 알립니다.
- 기존에 생성된 피드는 누락된 사진 근거를 복구할 수 없으므로 업데이트 후 `새 반응`으로 한 번 다시 생성해야 합니다.

## v0.9.60 피드 자연스러움·사진 안정성·보안 보강

- 장소 기반 글 60~70% 비율을 실제 출력의 `source`와 `factIds`로 검사하고 어긋나면 한 번 교정합니다.
- 정수 비율을 실제로 지키도록 가벼움은 5개, 기본은 8~9개부터 생성합니다.
- 과도한 말투 예시와 서로 겹치는 규칙을 줄여 반복적인 감탄사·유행어·관광 안내문 같은 출력을 완화했습니다.
- Pollinations 사진을 화면 진입 시 한 장씩 최소 5.5초 간격으로 불러오고, 429·503 또는 일시적 실패에 한 번 재시도합니다.
- 피드 모델이 출력한 `imagePrompt`는 사용하지 않습니다. RP가 포함되지 않는 Google 장소 조사 단계의 공개 장소 사진 단서로만 사진 문구를 만듭니다.
- 사진 요청은 자격 증명 미포함·no-referrer를 적용하며 JPEG/PNG/WebP와 8MB 이하 응답만 표시합니다.

## v0.9.59 장소 피드 사진·비율 보강

- 실제 장소 피드에서 장소 기반 글을 약 60~70%로 생성하도록 프롬프트와 재생성 목표를 조정했습니다.
- 장소 기반 글 일부에 Pollinations 사진을 표시합니다.
- 모델은 사진 URL을 만들 수 없고, 확장이 `image.pollinations.ai` 고정 주소로만 사진 URL을 조립합니다.
- 사진 프롬프트는 장소 기반 글에만 허용하며 URL·멘션·키처럼 보이는 문자열은 제거합니다.

## v0.9.58 실제 장소 판정 완화

- `groundingChunks`만 보던 검색 성공 판정을 검색어·자료·근거 전체로 넓혔습니다.
- `matched`와 `confidence`의 정확한 형식 일치를 요구하지 않습니다.
- 정확한 지점이 없으면 같은 이름 또는 주소 주변의 지역 정보도 사용할 수 있습니다.
- `factIds`가 부족하다는 이유로 생성된 피드 전체를 폐기하지 않습니다.

## v0.9.57 실제 장소 피드·주소 검색 보강

- Google 검색 단계에는 장소명과 주소만 보내고, 확인된 실제 정보 묶음을 별도 피드 생성 호출에 전달합니다.
- 피드 절반 이상의 `factIds` 반영을 검사하며 부족한 경우 한 번만 재생성합니다.
- 18초 이상 걸리면 진행 중 알림을 표시합니다.
- Photon이 결과를 못 찾거나 한글 검색어와 전혀 맞지 않을 때, 사용자 Enter 검색에서만 Nominatim을 보조로 호출합니다.
- 기존 기본 생성·Overpass 주변 보강·자동 Photon 지오코딩·역지오코딩은 유지합니다.

## v0.9.56 Vertex 요청 형식 수정

- Vertex Express 일반 호출과 Google 검색 보강 호출의 `contents`에 `role: user`를 명시했습니다.
- 기능과 설정 UI는 변경하지 않았습니다.

## v0.9.55 Vertex·설정창 정리

- 직접 API 연결을 Vertex AI Express 하나로 통합했습니다. 이전처럼 Vertex 키를 저장해도 시작 과정에서 삭제되는 문제를 고쳤습니다.
- Vertex 키는 PAW MAP 설정·DB·백업·안전 진단과 분리된 브라우저 로컬 저장소에 보관합니다. 서비스 계정 JSON은 지원하지 않습니다.
- 모델명은 직접 입력할 수 있고 Flash 모델 목록에서 빠르게 고를 수 있습니다. 기본값은 `gemini-3.7-flash`입니다.
- Vertex 키 연결에서 Google Search 보강을 사용할 수 있습니다. 장소명과 관련 프롬프트가 Google에 전달되고 연결된 Vertex 계정의 사용량·크레딧이 소모될 수 있습니다.
- `AI 기능 사용` 한 개로 외부 AI와 RP 공유 동의를 묶고, 이벤트·예정 일정 자동 기록도 한 개 스위치로 합쳤습니다.
- 표시 언어·생성 분량·현지 정보는 한 줄로 줄이고, 지도 디자인은 `ST 테마 따라가기`와 `기본 지도` 두 가지로 정리했습니다.
- Google 지도·길찾기 버튼은 `구글맵 연동`으로 묶었습니다. Street View는 이 설정과 관계없이 확정 좌표가 있으면 계속 사용할 수 있습니다.
- 안전 진단창과 복사 내용에서 마지막 LLM 원문 응답을 제거했습니다. API 키·프롬프트·채팅 원문·LLM 원문 응답은 진단 정보에 포함하지 않습니다.
- 커뮤니티 카드가 제3자 이미지 생성 서버에 키워드를 보내던 경로와 인라인 오류 이벤트를 제거했습니다.

아래 내용은 이전 버전에서 유지되는 기능 기록입니다.

## v0.9.50 핵심 회귀 수정

- 장소 자동 감지·등록을 기본 동작으로 유지했습니다. 정상적인 좌표 불명 장소는 현재 장소 30~150m 반경의 안정적인 `≈ 추정 핀`으로 표시됩니다.
- 지도 검색창은 등록 장소를 먼저 검색하고, 결과가 없을 때 Enter를 누르면 Photon/OpenStreetMap 검색으로 이어집니다. 입력 중에는 외부 요청을 보내지 않습니다.
- `Room`, `Bedroom`, `Kitchen`, `방`, `침실` 같은 단독 내부 장소는 현재 상위 장소가 있을 때만 하위 장소로 자동 등록합니다. 상위 장소가 없으면 Photon에 보내지 않고 후보로 보류합니다.
- `Drift point` 등 감사·메타 로그를 이벤트 추출 전에 제외하고, 모바일 이벤트 카드 펼침·닫기를 수정했습니다.
- 왼쪽으로 돌아가던 지도 핀을 아래를 향하도록 수정했습니다.

## RP 경로·Google 링크·커뮤니티

- `RP 경로`는 등록된 두 장소 사이에 직선 경로·거리·예상 도보 시간을 표시합니다. 외부 경로 API를 쓰지 않고 DB·이동 기록·거리 관계에 저장하지 않는 세션 전용 표시입니다.
- Google 지도·Google 길찾기·Street View 링크는 기본 OFF이며 설정에서 켰 때만 버튼이 표시됩니다. 기능 코드는 삭제하지 않았습니다.
- 커뮤니티는 `기본 생성`과 `Overpass 주변 정보 보강 생성`을 선택할 수 있습니다. 보강 방식은 확정 좌표만 소수점 4자리로 반올림해 Overpass에 보내며 `≈ 추정 핀`에서는 차단됩니다.
- 두 커뮤니티 생성 방식 모두 선택한 SillyTavern 연결 프로필의 AI를 사용합니다. PAW MAP이 별도 카드나 결제 계정을 연결하지 않으며, 해당 공급자 계정의 토큰·크레딧·사용량이 차감됩니다.
- 연결 프로필로 전송하는 임시 프롬프트와 선택형 일반 채팅 지도 맥락에서 흔한 API 키·비밀 할당값, access/refresh/id token, Basic·Bearer 인증값, PEM 개인키 형태를 마스킹합니다. system 채팅과 프로필 프리셋·지시문은 유틸리티 생성 맥락에서 제외하며, 저장된 RP 원문과 백업 데이터는 임의로 바꾸지 않습니다.

## v0.9.49 핵심 자동 등록 복구

- v0.9.47에서 잘못 제거한 `장소 감지 → 자동 등록·이동`을 기본 동작으로 복구했습니다.
- 오탐 필터 강화는 유지합니다. 시스템 문구·대명사·문장 조각·사물 복합어 등 거부 대상은 자동 등록되지 않습니다.
- 새 장소에 실제 좌표가 없고 현재 장소에는 좌표가 있으면, 현재 장소 30~150m 반경에 `≈ 추정 핀`을 자동 생성합니다.
- 추정 핀은 로컬 계산만 사용하며 실제 주소·Street View 좌표로 취급하지 않습니다.
- 후보함은 자동 등록을 대체하는 강제 절차가 아니라 사용자가 선택할 수 있는 확인 모드로 돌렸습니다.
- 외부 AI·RP 공유·감지 장소명 Photon 전송은 계속 별도 동의가 필요합니다.

## v0.9.48 주소·모바일 팝업 회귀 수정

- 한글·영문 주소 검색이 모두 `결과 없음`으로 끝나던 Photon 언어 파라미터 오류를 수정했습니다.
- 지도를 직접 꾹 눌러 등록하거나 핀을 직접 옮기면 해당 RP 좌표의 주소를 자동 입력합니다. 감지된 채팅 장소명의 백그라운드 검색은 계속 기본 OFF입니다.
- 의심 장소 점검의 `선택 장소 삭제` 창을 모바일 화면의 하단과 안전 영역 안에 맞추고, 닫기 버튼·바깥 탭·Esc 닫기를 보강했습니다.
- 외부 AI는 새 결제 계정/카드를 연결하지 않으며, 사용자가 이미 선택한 SillyTavern 연결 프로필 공급자 계정의 토큰·크레딧·사용량을 씁니다. 실제 청구 방식은 그 기존 계정 설정을 따릅니다.

## 눈에 보이는 변경

- 상단 `🪧` 버튼과 숫자 배지로 장소 후보함을 엽니다.
- 후보 카드는 탐지 출처·신뢰도·판정 이유·일시적 근거를 보여주며, 이름 수정 후 기존 장소 연결/새 등록/이번만 무시/계속 무시를 선택할 수 있습니다.
- 설정과 데이터 메뉴에 `🧹 의심 장소 점검`을 추가했습니다. 항목은 기본 미선택이며, 사용자가 체크하고 재확인해야만 삭제됩니다.
- 같은 점검 화면에서 체크한 기존 핀의 좌표·주소만 초기화할 수 있습니다. 장소·이벤트·이동·약도 위치는 남습니다.
- `Google 지도 링크 표시`를 켰 때만 장소 상세와 모바일 바텀시트에 `Google에서 보기`, `Google 길찾기`, `Street View` 버튼이 나타납니다.

## 오탐 차단 경계

기본 자동 모드: `채팅 탐지 → 오탐 필터 → 영구 장소/이동`

선택 확인 모드: `채팅 탐지 → 메모리 후보 → 사용자 승인 → 영구 장소/이동`

확인 모드의 승인 전 후보는 IndexedDB와 백업에 들어가지 않습니다. 자동 모드에서 필터를 통과한 새 장소는 즉시 등록되며, 실제 좌표가 없으면 현재 장소 주변 추정 핀으로 표시됩니다. 좌표 전체 쌍 자동 연결은 계속 만들지 않습니다.

## Google 연동 범위

Google 데이터를 PAW MAP 내부로 가져오지 않습니다. 공식 [Maps URLs](https://developers.google.com/maps/documentation/urls/get-started) 형식으로 사용자가 누른 순간 새 탭을 여는 보조 기능만 제공합니다. API 키·SDK·Places/Routes API·Google Cloud 결제 계정은 사용하지 않습니다.

Google 길찾기는 저장된 출발지와 목적지를 모두 넣습니다. 출발지를 생략해 Google이 기기 위치를 대신 쓰는 동작을 허용하지 않습니다. 고정 HTTPS 호스트, 동작별 파라미터 허용목록, `noopener,noreferrer`를 적용했습니다.

## 확인한 항목

- 스크린샷 오탐 이름과 대명사/한 글자/문장 조각 거부
- `Crawl home`의 Home 연결 제안, `Club 앞`의 상대 위치 판정
- `병원` 안의 `원`, `상점` 안의 `점`, `bar of soap`, `office chair`, `club sandwich`, `office party` 부분 오탐 방지
- `left Club`/`클럽에서 나왔다`를 도착으로 뒤집지 않고 뒤에 명시된 실제 목적지만 선택하는지 확인
- 후보가 메모리에만 남고 계속 무시한 정규화 이름만 로컬 설정에 저장되는지 확인
- Google URL의 호스트·출발지/목적지·Street View 좌표·키/토큰/UTM 차단 확인
- 백업 비밀명 필드 제거, 자동 등록 필터 적용, 추정 핀의 반경·안정성·Street View 차단 확인
- 등록 장소 결과가 없는 Enter에서만 Photon이 호출되고, 부모 없는 `Room` 후보는 Photon·DB에 자동 승격되지 않으며 부모가 있으면 실제 하위 장소로 자동 등록되는지 확인
- `Drift point` 단독 메타 문구가 이벤트·AI 요청을 만들지 않고 모바일 카드가 펼침·닫기 되는지 확인
- RP 경로가 외부 요청·DB 저장 없이 동작하고 Google 링크가 기본 OFF인지 확인
- 커뮤니티 기본 생성은 Overpass를 호출하지 않고, 보강 생성은 확정 좌표에서만 Overpass를 1회 호출하는지 확인
- 모든 JavaScript 구문 검사와 `node tests/security-regression.mjs` 통과

SillyTavern 실제 화면의 장시간 모바일 상호작용과 각 외부 서비스 장애 상황은 자동 테스트로 완전히 재현하지 못했습니다. 설치 전 기존 데이터 백업을 보관하고, v0.9.50은 별도 베타로 먼저 확인하세요.
